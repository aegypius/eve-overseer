language: node_js
node_js:
  - '6'
services:
  - mongodb
install:
  - npm install
  - for service in services/*; do sh -c 'cd $service; npm install'; done
script:
  - npm test
  - for service in services/*; do sh -c 'cd $service; npm test'; done
