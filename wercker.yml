box: wercker/nodejs
services:
  - wercker/mongodb
build:
  steps:
    - npm-install
    - script:
        name: Loading static data into database
        code: |-
          DEBUG=* ./bin/overseer database:upgrade
    - npm-test
    - script:
        name: Build production assets
        code: |-
          npm run build
deploy:
  steps:
    - heroku-deploy:
        key-name: radiant-bastion-3939
